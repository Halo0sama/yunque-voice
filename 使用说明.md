# 云雀·私人助理

## 愿景

云雀不想做那种叫一声才应一声的语音助手。它更希望成为一个真正待在身边的私人助理：你在说话时，它在听；它不打扰你，只在真正值得的时候开口；它会记住你说过的事，慢慢理解你的性格和习惯，甚至逐渐认识你身边的人，帮你整理这些人之间的关系。说到底，云雀相信真正好的助理应该是主动的、有记忆的、懂人情的，而不是一个只会等待指令的问答机器。

## 它做了什么：表面能力

云雀最特别的一点是全天候聆听与自主决定。你不需要每次喊它的名字，它会一直旁听，自己判断当前是否值得回应。你打断它时，它会记录漏听内容，避免错过你说的话。不方便让它出声的场合，还有“仅聆听”模式，只听不说但记录照常。它有一份“关于我”画像文档，会随着时间自动沉淀你的兴趣、性格、习惯、偏好和经历——没有预设栏目，云雀每晚在压缩对话时顺便维护它（合并重复、消解矛盾、丢弃琐事），空间不够时自动扩容（上限 2000 字），全部可编辑。它还会识别你身边的人，为每个人建立独立档案，并整理成关系图谱；你可以重命名、删除或合并人物。声纹让云雀能区分你和别人，声音库支持频率、时间排序，以及分类和日期范围筛选。除此之外，它有长期记忆：每听到一句话就写入阿里云百炼的云端向量记忆库，随存随查，也可以通过蓝牙耳机功能键一键开启聆听。对于开发者，它还提供了本地 CLI/MCP 接口。

## 背后的机制：它怎么做到这些

云雀的聆听依赖一个常驻前台服务和麦克风录音。它通过 AudioRecord 以 16kHz 采样持续采集，并会在蓝牙耳机连接时自动切换到蓝牙 SCO 通道，这样使用 OWS 一类蓝牙耳机时也能稳定收音。采集到声音后，它会经过一个能量检测的简易 VAD 判断是否有人说话，再进入队列等待识别，避免把静音和噪声浪费给模型。

识别环节使用阿里云百炼的 Qwen3-ASR-Flash，通过 OpenAI 兼容的 HTTP 接口调用。识别之后的“思考”由对话模型完成，支持两家供应商，在设置 →“AI 与接口”里一键切换：DeepSeek（deepseek-v4-flash）或智谱 GLM（glm-5.3-flash），两者都是 OpenAI 兼容协议，工具调用、旁听决策、记忆压缩全部无差别工作，Key 填对应家的即可。识别出的文本会带上说话人信息，并写入本地对话记录。每一步都允许被中断，中断时云雀会把当前未说完的内容截取成“漏听文本”，并关联回对应的云端回答记录，这也是你在对话里看到橙色漏听标注的来源。

决定要不要开口，背后不是简单规则。云雀会把当前文本、最近的对话、记忆检索结果以及你的个人信息一起发给 DeepSeek V4 Flash，由模型判断该沉默还是回应，并在需要时返回一句适合直接朗读的短回答。不同聆听模式下，它处理“直接回答”和“只响应云雀唤醒”的策略也不同。为了让回答更贴合你，它还会从数据库里检索相关记忆，结合时间衰减和关键词命中，给你更有人情味的回应。

说话人识别是云雀最有技术含量的部分之一。它有两套路径：本地声纹特征聚类，根据短时间内声音的 RMS、过零率和基频等特征把不同说话人分开；以及云端文件转写加多人说话人分离（DIAR），需要你在设置里填写阿里云百炼的业务空间 ID。云端结果会以“云端说话人N”的形式回写本地档案，并且以云端为主自动把本地声纹合并进云端主档案，尽量不产生重复条目。如果云端把同一个真人误判成两个声音，你可以在“身边的人”里把两个声音命名成同一个名字，云雀会弹二次确认，确认后自动合并。

记忆系统走纯云端路线。每听到一句话（不管是你说的、别人说的、还是你在对话面板里打的字），云雀会按对话节奏攒成簇、一次性写入阿里云百炼的云端向量记忆库，由云端 AI 自主提炼哪些值得长期记住、如何合并去重——没有本地的关键词硬规则，“是否记住”这件事完全交给你在百炼控制台配置的记忆规则决定；想让它更可靠地保留明确交代的事，可以在控制台的记忆片段规则指令里加一句“用户明确要求记住的内容必须原样保留”。云雀需要时按语义检索最相关的几条注入给模型。此外还有一层“工作记忆”：近期的对话原话和一份滚动摘要会始终带在身上，服务重启、手机重启都不会丢——聊天可以从上次断掉的地方自然接下去；每天它还会把更早的对话折叠进摘要，并把值得长期记住的新事实自动存入云端记忆库。记忆板块就是云端记忆库的直连视图：可以语义搜索（问“天气”也能搜到“台风”）、手动添加、逐条删除；首次升级会自动把本地旧记忆迁移上云。断网时的写入会先暂存在手机里，网络恢复后自动补传，一条不丢。“关于我”则是一份画像文档：与工作记忆摘要共用每晚那次压缩调用一并维护——摘要记“最近发生了什么”，画像记“你是个怎样的人”，云雀自主决定什么值得沉淀进画像，预算会随内容自然增长。你在“关于我”页面看到的和能编辑的，就是这份画像本身。

人物关系由单独的关系提取器负责。它会从带说话人标签的对话里推断真实姓名和关系，并保存证据原文，所以你在人物档案里可以看到每条关系的依据。关系图谱上的节点默认只展示已命名的声音，未命名声音留在声音列表中等待你去分类。

底层数据主要存放在手机本地 SQLite 数据库中，包括说话人、对话、人物关系、关系图位置和“关于我”记录（长期记忆单独存放在阿里云百炼的云端记忆库，见上文）；API Key、业务空间 ID、记忆库 ID、姓名、主题模式等设置则存放在 SharedPreferences。本地 CLI/MCP 服务运行在 127.0.0.1:8766，提供健康检查、记忆（列表/搜索/添加/删除）、对话、说话人、关系、漏听等接口，以及 JSON-RPC 形式的 MCP 工具调用（含 search_memory、delete_memory），方便你把它接入自己的脚本或自动化工具。

## 隐私边界

对话、声纹、人物关系、“关于我”等数据仍只保存在手机本地；唯一上云的是长期记忆：它明确存储在阿里云百炼的云端记忆库里，只绑定你这台安装随机生成的用户 ID，随时可以在记忆板块查看、搜索或删除任何一条，设置里也可以整体关闭云端记忆（关闭后既不写入也不读取）。使用云端记忆需要先配置阿里云百炼 API Key、业务空间 ID 和记忆库 ID。APK 安装包本身不包含任何个人数据，所以分享 APK 不会泄露你的对话、记忆、声纹或 Key。请不要分享手机上的数据库、日志和配置文件，那些才是真正包含隐私的地方。

## 系统语音助手注册与绑定

安装云雀·私人助理后，还需要把它注册为系统语音助手，才能在系统快捷键、蓝牙耳机功能键等场景直接唤起。

在 Android 原生系统上，进入“设置 → 搜索默认应用设置 → 语音助手”，选择“云雀·私人助理”即可。在小米 HyperOS/MIUI 上，进入“设置 → 语音助手”，将“系统快捷键唤起”和“语音蓝牙唤起”都选择为“云雀·私人助理”。如果列表中没有出现，请重启一次手机或重新安装 APK 后再查看。绑定后，系统快捷键会打开云雀的语音界面，蓝牙耳机功能键会直接开启全天聆听。

相关参考网址：

- Android 语音交互服务文档：https://developer.android.com/reference/android/service/voice/VoiceInteractionService
- 阿里云百炼（语音识别 / 语音合成 / 声音复刻 / 业务空间）：https://help.aliyun.com/zh/model-studio/
- Operit AI（工具调用与手机自动化联动）：https://github.com/AAswordman/Operit

## 耳机功能键与通知栏控制

蓝牙耳机功能键默认用于切换全天聆听的起停，云雀会通过系统语音交互服务、小米广播和语音助手 Activity 多条路径尽量接收。只是因为小米私有的“语音蓝牙唤起”列表不一定收录第三方应用，部分机型上后续按键可能不会送达云雀。如果耳机键停止失效，可以用通知栏来控制：在设置里打开“通知栏控制”，可以分别开启“点击停止聆听”和“点击打断播报”。开启后，主通知第一行是云雀的名称，第二行写着“点击停止聆听”，点整条通知就会停止；另一条独立通知同样以云雀为名，第二行写着“点击打断播报”，点击即可打断当前播报。这两个开关默认关闭，开启时会请求系统通知权限。

## 仅聆听

有些场合不方便让手机开口：没戴耳机、正在开会，或者周围有人在休息。这时可以打开“仅聆听”，主界面“状态”卡片里有“仅聆听（保持安静）”按钮，运行中的通知栏上也有一个“仅聆听”操作，不用解锁手机就能一键切换（点击开关本身不会跳转任何页面）。

开启后，聆听、识别、说话人区分、对话记录、记忆和关系提炼都照常进行。它有两档：默认的“云雀有话要说”开启时，云雀照常判断该不该开口，只是回复不语音播报，而是变成文字出现在“对话”面板里，并弹一条轻通知，点开直达面板；在设置 →“仅聆听与对话”里把它关掉，则完全沉默、跳过判断、零消耗。开启时通知会显示仅聆听状态，独立的“点击打断播报”通知会自动隐藏；想恢复语音，再点一次“关闭仅聆听（恢复播报）”即可。这个开关会记住状态，下次开始聆听时仍然生效。

## 对话面板

底栏的“对话”面板就是和云雀聊天的地方：打字发消息，气泡式回复。这里和语音聆听共用同一段对话与记忆——你打进去的话按聆听模式同样的规则上云（该合簇合簇、说“记住”原样保存），云雀回答时也能引用耳边刚发生的事和长期记忆，语音和文字切换完全无缝。仅聆听期间云雀的“说话”也显示在这里。

## 人物合并、重命名与样本试听

“身边的人”里支持按频率、时间、七天或自定义日期筛选，筛选区是一排纯文字选项，不占空间。长按某个声音，可以命名或重命名，也可以删除。更重要的是，界面里提供了一个“随机播放 ta 的声音”的按钮，可以试听这个说话人最近一段真实录音，方便辨认这个人到底是谁。命名时如果输入的名字已经存在，云雀会先弹二次确认，确认后再把两者合并；合并会连同对话、关于这个人的记录、关系和图谱一起归到同一个主档案。云端为主合并开启后，列表和关系图谱只显示主档案，旧声纹会自动隐藏。

## 角色卡与称呼

默认的“云雀”角色卡把用户视为“主人”，角色设定和提示词里都明确写着“用户称呼：主人（不要直呼其名）”。你的姓名只作为云雀了解你的背景知识，不会用来称呼你。角色卡本身支持 Tavern 的 JSON 和 PNG 导入导出，也支持世界书的导入导出。

## 删除安全

所有删除操作都带二次确认，包括记忆、关于我、人物档案记录、角色卡、世界书与条目，以及声纹，避免误删。

## 适合的场景

它适合希望有一个持续陪伴、不需要频繁唤醒的助理的人，尤其适合在居家或办公室环境里使用。如果你希望助理记得你的偏好和经历，如果你想整理自己认识的人和这些人之间的关系，如果你想用语音快速记录并查询记忆，又或者你想把它作为本地助手接入自己的工具链，云雀都会是一个很好的选择。

## 一句话总结

它不是一个问答机器，而是一个越来越懂你的身边助理。
